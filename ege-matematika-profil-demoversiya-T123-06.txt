<script>
(function(){"use strict";
var root=document.getElementById("ege-math-profile-demo-2026"),TASKS=[].concat(window.EMPROF_TASKS_1||[],window.EMPROF_TASKS_2||[],window.EMPROF_TASKS_3||[],window.EMPROF_TASKS_4||[]),KEY="eksamio_ege_math_profile_demo_2026_v2",DURATION=235*60*1000,timer=null;
function fresh(){return{status:"idle",startedAt:0,endsAt:0,completedAt:0,current:1,variants:{},answers:{},drafts:{},flags:{},self:{}}}var state=fresh();try{var s=localStorage.getItem(KEY);if(s)state=Object.assign(fresh(),JSON.parse(s))}catch(e){}
function save(){try{localStorage.setItem(KEY,JSON.stringify(state))}catch(e){}}
function esc(s){return String(s==null?"":s).replace(/[&<>\"]/g,function(c){return{"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]})}function by(id){return document.getElementById(id)}
function groups(n){return TASKS.filter(function(t){return t.number===n})}function ensure(){for(var n=1;n<=19;n++){if(!state.variants[n]){var g=groups(n);state.variants[n]=g[Math.floor(Math.random()*g.length)].variant}}save()}function task(n){return TASKS.find(function(t){return t.number===n&&t.variant===Number(state.variants[n])})}
function norm(v){return String(v==null?"":v).replace(',', '.')}function valid(v){return /^-?(?:\d+|\d+\.\d+)$/.test(norm(v))}function score(t,v){if(t.kind!=="numeric"||!valid(v))return 0;return Math.abs(Number(norm(v))-Number(t.answer))<1e-10?1:0}function filled(n){var t=task(n),v=state.answers[n];return t.kind==="numeric"?String(v||"").length>0:String(v||"").trim().length>0}
function update(){root.setAttribute("data-state",state.status);by("emprof-resume").classList.toggle("emprof-hidden",state.status!=="running");by("emprof-reset-start").classList.toggle("emprof-hidden",state.status==="idle");}
function buildNav(){var w=by("emprof-nav");w.innerHTML="";for(let n=1;n<=19;n++){let b=document.createElement("button");b.textContent=n;b.type="button";b.setAttribute("aria-current",n===state.current?"true":"false");b.dataset.filled=filled(n)?"true":"false";b.dataset.flag=state.flags[n]?"true":"false";b.onclick=function(){state.current=n;save();render()};w.appendChild(b)}}
function field(t){if(t.kind==="numeric")return '<div class="emprof-answer"><label for="emprof-answer">Ответ</label><input id="emprof-answer" inputmode="decimal" value="'+esc(state.answers[t.number]||"")+'"><div id="emprof-validation" class="emprof-mini">Целое число или конечная десятичная дробь.</div></div>';return '<div class="emprof-answer"><label for="emprof-answer">Полное решение и ответ</label><textarea id="emprof-answer">'+esc(state.answers[t.number]||"")+'</textarea></div>'}
function render(){var t=task(state.current),s=by("emprof-stage");s.innerHTML='<div class="emprof-head"><div><div class="emprof-number">Задание '+t.number+' из 19 · официальный пример '+t.variant+'</div><h2>'+esc(t.title)+'</h2></div><span class="ep-pill">'+t.maxScore+' '+(t.maxScore===1?'балл':'балла')+'</span></div><div class="emprof-prompt">'+t.promptHtml+'</div>'+field(t)+'<details class="emprof-draft"><summary>Черновик</summary><textarea id="emprof-draft">'+esc(state.drafts[t.number]||"")+'</textarea></details><div class="emprof-task-actions"><div><button id="emprof-prev" class="ep-button ep-button--secondary ep-button--small" '+(t.number===1?'disabled':'')+'>Назад</button> <button id="emprof-next" class="ep-button ep-button--small">'+(t.number===19?'Завершить':'Далее')+'</button></div><button id="emprof-flag" class="ep-button ep-button--secondary ep-button--small">'+(state.flags[t.number]?'Снять отметку':'Вернуться позже')+'</button></div>';
var a=by("emprof-answer");a.oninput=function(){state.answers[t.number]=a.value;save();buildNav();if(t.kind==="numeric")by("emprof-validation").textContent=!a.value||valid(a.value)?"Ответ сохраняется автоматически.":"Допустимы только цифры, минус и десятичная запятая или точка."};by("emprof-draft").oninput=function(e){state.drafts[t.number]=e.target.value;save()};by("emprof-prev").onclick=function(){if(state.current>1){state.current--;save();render()}};by("emprof-next").onclick=function(){if(state.current<19){state.current++;save();render()}else finish(false)};by("emprof-flag").onclick=function(){state.flags[t.number]=!state.flags[t.number];save();render()};bindZoom(s);buildNav();by("emprof-progress").style.width=(Object.keys(state.answers).filter(function(k){return filled(Number(k))}).length/19*100)+"%"}
function format(ms){ms=Math.max(0,ms);var sec=Math.floor(ms/1000),h=Math.floor(sec/3600),m=Math.floor(sec%3600/60),s=sec%60;return [h,m,s].map(function(x){return String(x).padStart(2,'0')}).join(':')}
function tick(){var left=state.endsAt-Date.now();by("emprof-timer").textContent=format(left);if(left<=0)finish(true)}function startTimer(){clearInterval(timer);tick();timer=setInterval(tick,1000)}
function start(){state=fresh();ensure();state.status="running";state.startedAt=Date.now();state.endsAt=state.startedAt+DURATION;save();update();buildNav();render();startTimer()}function resume(){ensure();if(state.endsAt<=Date.now())return finish(true);state.status="running";update();buildNav();render();startTimer()}function reset(){if(!confirm("Удалить попытку и начать заново?"))return;try{localStorage.removeItem(KEY)}catch(e){}state=fresh();update()}
function finish(auto){if(!auto&&!confirm("Завершить экзамен? После этого ответы нельзя изменить."))return;clearInterval(timer);state.status="finished";state.completedAt=Date.now();save();update();results()}
function results(){by("emprof-date").textContent=new Date(state.completedAt).toLocaleString('ru-RU');var ss=0;for(var n=1;n<=12;n++)ss+=score(task(n),state.answers[n]);by("emprof-short-score").textContent=ss;var self=by("emprof-self");self.innerHTML="";for(let n=13;n<=19;n++){let t=task(n),r=document.createElement('div');r.className='emprof-self-row';r.innerHTML='<div><strong>Задание '+n+' · пример '+t.variant+'</strong><div class="emprof-mini">Максимум '+t.maxScore+'</div></div><select data-n="'+n+'"><option value="">Выберите балл</option>'+Array.from({length:t.maxScore+1},function(_,i){return '<option value="'+i+'" '+(String(state.self[n])===String(i)?'selected':'')+'>'+i+'</option>'}).join('')+'</select>';self.appendChild(r)}self.onchange=function(e){if(e.target.matches('select')){state.self[e.target.dataset.n]=e.target.value;save();totals()}};review();totals()}
function totals(){var ready=true,sum=0;for(var n=13;n<=19;n++){if(state.self[n]==null||state.self[n]==='')ready=false;else sum+=Number(state.self[n])}by("emprof-ext-score").textContent=ready?sum:'—';by("emprof-total-score").textContent=ready?(Number(by("emprof-short-score").textContent)+sum):'—'}
function review(){var w=by("emprof-review");w.innerHTML="";for(var n=1;n<=19;n++){var t=task(n),a=document.createElement('article');if(t.kind==='numeric'){var pts=score(t,state.answers[n]);a.innerHTML='<h3>Задание '+n+' · пример '+t.variant+' · '+pts+'/1</h3><p>Ваш ответ: <strong>'+esc(state.answers[n]||'—')+'</strong></p><p>Ответ ФИПИ: <strong>'+esc(t.answerDisplay)+'</strong></p>'}else{a.innerHTML='<h3>Задание '+n+' · пример '+t.variant+'</h3><details><summary>Возможное решение</summary><div class="emprof-solution">'+t.solutionHtml+'</div></details><details><summary>Ориентиры оценивания</summary>'+t.criteriaHtml+'</details>'}w.appendChild(a)}}
function bindZoom(scope){scope.querySelectorAll('.emprof-zoom').forEach(function(b){b.onclick=function(){var img=b.closest('figure').querySelector('img');by('emprof-modal-body').innerHTML='<img src="'+img.src+'" alt="'+esc(img.alt)+'">';by('emprof-modal').dataset.open='true'}})}function close(){by('emprof-modal').dataset.open='false'}
function reference(){by('emprof-modal-body').innerHTML='<h2>Справочные материалы</h2><div class="emprof-formula">sin 2α = 2sin α cos α<br>cos 2α = cos²α − sin²α<br>sin(α+β)=sin α cos β+cos α sin β<br>cos(α+β)=cos α cos β−sin α sin β</div>';by('emprof-modal').dataset.open='true'}
by('emprof-start').onclick=start;by('emprof-resume').onclick=resume;by('emprof-reset-start').onclick=reset;by('emprof-reset-result').onclick=reset;by('emprof-finish').onclick=function(){finish(false)};by('emprof-reference').onclick=reference;by('emprof-modal-close').onclick=close;by('emprof-modal').onclick=function(e){if(e.target===this)close()};
window.__emprofTest={tasks:TASKS,getTask:function(n,v){return TASKS.find(function(t){return t.number===n&&t.variant===v})},score:score,valid:valid,state:function(){return JSON.parse(JSON.stringify(state))},setTask:function(n,v){ensure();state.status='running';state.current=n;state.variants[n]=v;update();buildNav();render()},start:start,finish:function(){finish(true)}};
ensure();update();if(state.status==='running')resume();if(state.status==='finished')results();
})();
</script>